services:
  - type: web
    name: diabetic-retinopathy-backend
    env: python
    plan: free
    branch: master
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 1 -k eventlet -b 0.0.0.0:$PORT app.main:app
    envVars:
      - key: MONGODB_URI
        value: ""
