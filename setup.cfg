[metadata]
name = dr-detect-ai

[options]
packages = find:
python_requires = >=3.11
install_requires =
    flask
    tensorflow
    numpy
    opencv-python

[options.packages.find]
where = .
include = app*, src*
